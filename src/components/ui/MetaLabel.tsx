import { Text } from "@chakra-ui/react";
import type { TextProps } from "@chakra-ui/react";

export default function MetaLabel(props: TextProps) {
  return <Text textStyle="metaLabel" {...props} />;
}
